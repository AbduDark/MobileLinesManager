<UserControl x:Class="MobileLinesManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:MobileLinesManager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <conv:HexToBrushConverter x:Key="HexToBrushConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            
            <!-- Welcome Header -->
            <Border CornerRadius="10" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#0078D4" Offset="0"/>
                        <GradientStop Color="#0063B1" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Margin="30,25">
                    <TextBlock Text="مرحباً بك في نظام إدارة خطوط الموبايل" 
                               FontSize="32" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock Text="اختر المشغل للبدء في إدارة المجموعات والخطوط" 
                               FontSize="16" 
                               Foreground="White" 
                               Opacity="0.9"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Alerts Section -->
            <Border Style="{StaticResource ModernCard}" 
                    Background="#FFF4CE" 
                    BorderBrush="#FFB900" 
                    BorderThickness="2"
                    Margin="0,20,0,20"
                    Visibility="{Binding HasAlerts, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="⚠️" FontSize="24" Margin="0,0,10,0"/>
                        <TextBlock Text="التنبيهات الهامة" 
                                   FontSize="20" 
                                   FontWeight="Bold"
                                   Foreground="#8B4513"/>
                        <Border Background="#E74856" 
                                CornerRadius="12" 
                                Padding="8,4" 
                                MinWidth="30"
                                Margin="15,0,0,0">
                            <TextBlock Text="{Binding AlertCount}" 
                                       Foreground="White" 
                                       FontWeight="Bold" 
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                    
                    <ItemsControl ItemsSource="{Binding RecentAlerts}" MaxHeight="200">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" 
                                        CornerRadius="6" 
                                        Padding="12" 
                                        Margin="0,0,0,8"
                                        BorderBrush="#FFE5B4" 
                                        BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Message}" 
                                                   TextWrapping="Wrap"
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Line.PhoneNumber, StringFormat='الخط: {0}'}" 
                                                   Foreground="#666" 
                                                   FontSize="12"
                                                   Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Operators Selection - Big Colorful Buttons -->
            <TextBlock Text="اختر المشغل" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,10,0,20"
                       HorizontalAlignment="Center"/>

            <UniformGrid Rows="2" Columns="2" Margin="0,0,0,30">
                
                <!-- Vodafone Button -->
                <Button Command="{Binding NavigateToOperatorGroupsCommand}" 
                        CommandParameter="2"
                        Style="{StaticResource OperatorIconButton}"
                        Background="{StaticResource VodafoneBrush}">
                    <StackPanel>
                        <Image Source="/Resources/Images/vodafone_logo.png" 
                               Width="100" 
                               Height="100" 
                               Margin="0,0,0,15"/>
                        <TextBlock Text="فودافون" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding VodafoneGroupsCount, StringFormat='{}{0} مجموعة'}" 
                                   FontSize="16" 
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"
                                   Opacity="0.9"/>
                    </StackPanel>
                </Button>

                <!-- Etisalat Button -->
                <Button Command="{Binding NavigateToOperatorGroupsCommand}" 
                        CommandParameter="1"
                        Style="{StaticResource OperatorIconButton}"
                        Background="{StaticResource EtisalatBrush}">
                    <StackPanel>
                        <Image Source="/Resources/Images/etisalat_logo.png" 
                               Width="100" 
                               Height="100" 
                               Margin="0,0,0,15"/>
                        <TextBlock Text="اتصالات" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding EtisalatGroupsCount, StringFormat='{}{0} مجموعة'}" 
                                   FontSize="16" 
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"
                                   Opacity="0.9"/>
                    </StackPanel>
                </Button>

                <!-- WE Button -->
                <Button Command="{Binding NavigateToOperatorGroupsCommand}" 
                        CommandParameter="3"
                        Style="{StaticResource OperatorIconButton}"
                        Background="{StaticResource WEBrush}">
                    <StackPanel>
                        <Image Source="/Resources/Images/we_logo.png" 
                               Width="100" 
                               Height="100" 
                               Margin="0,0,0,15"/>
                        <TextBlock Text="وي" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding WEGroupsCount, StringFormat='{}{0} مجموعة'}" 
                                   FontSize="16" 
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"
                                   Opacity="0.9"/>
                    </StackPanel>
                </Button>

                <!-- Orange Button -->
                <Button Command="{Binding NavigateToOperatorGroupsCommand}" 
                        CommandParameter="4"
                        Style="{StaticResource OperatorIconButton}"
                        Background="{StaticResource OrangeBrush}">
                    <StackPanel>
                        <Image Source="/Resources/Images/orange_logo.png" 
                               Width="100" 
                               Height="100" 
                               Margin="0,0,0,15"/>
                        <TextBlock Text="أورانج" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding OrangeGroupsCount, StringFormat='{}{0} مجموعة'}" 
                                   FontSize="16" 
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"
                                   Opacity="0.9"/>
                    </StackPanel>
                </Button>

            </UniformGrid>

            <!-- Quick Stats Cards -->
            <TextBlock Text="إحصائيات سريعة" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Margin="0,20,0,15"/>

            <UniformGrid Columns="4" Rows="1" Margin="0,0,0,20">
                
                <!-- Total Groups -->
                <Border Style="{StaticResource ModernCard}" 
                        Background="#E3F2FD" 
                        BorderBrush="#2196F3" 
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="📦" 
                                   FontSize="40" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="إجمالي المجموعات" 
                                   FontSize="14" 
                                   Foreground="#1976D2" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding TotalGroups}" 
                                   FontSize="32" 
                                   FontWeight="Bold" 
                                   Foreground="#0D47A1"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Total Lines -->
                <Border Style="{StaticResource ModernCard}" 
                        Background="#E8F5E9" 
                        BorderBrush="#4CAF50" 
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="📱" 
                                   FontSize="40" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="إجمالي الخطوط" 
                                   FontSize="14" 
                                   Foreground="#388E3C" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding TotalLines}" 
                                   FontSize="32" 
                                   FontWeight="Bold" 
                                   Foreground="#1B5E20"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Assigned Lines -->
                <Border Style="{StaticResource ModernCard}" 
                        Background="#FFF3E0" 
                        BorderBrush="#FF9800" 
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="✓" 
                                   FontSize="40" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="الخطوط المسلمة" 
                                   FontSize="14" 
                                   Foreground="#F57C00" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding AssignedLines}" 
                                   FontSize="32" 
                                   FontWeight="Bold" 
                                   Foreground="#E65100"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Available Lines -->
                <Border Style="{StaticResource ModernCard}" 
                        Background="#F3E5F5" 
                        BorderBrush="#9C27B0" 
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="○" 
                                   FontSize="40" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="الخطوط المتاحة" 
                                   FontSize="14" 
                                   Foreground="#7B1FA2" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding AvailableLines}" 
                                   FontSize="32" 
                                   FontWeight="Bold" 
                                   Foreground="#4A148C"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

            </UniformGrid>

            <!-- Refresh Button -->
            <Button Content="🔄 تحديث البيانات" 
                    Command="{Binding RefreshDashboardCommand}" 
                    Style="{StaticResource PrimaryButton}" 
                    Margin="0,10,0,0" 
                    HorizontalAlignment="Center" 
                    Padding="35,12"
                    FontSize="16"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
