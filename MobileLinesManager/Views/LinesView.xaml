
<UserControl x:Class="MobileLinesManager.Views.LinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Search and Actions Bar -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                            Width="300" 
                            Padding="8" 
                            VerticalContentAlignment="Center"
                            Margin="0,0,10,0">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="White" 
                                                   BorderBrush="#CCCCCC" 
                                                   BorderThickness="1" 
                                                   CornerRadius="4">
                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                            Padding="8"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <Button Content="بحث" 
                           Command="{Binding SearchCommand}" 
                           Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="استيراد CSV" 
                           Command="{Binding ImportCSVCommand}" 
                           Style="{StaticResource SecondaryButton}" 
                           Margin="0,0,5,0"/>
                    <Button Content="مسح QR" 
                           Command="{Binding ImportQRCommand}" 
                           Style="{StaticResource SecondaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Lines Grid -->
        <DataGrid Grid.Row="1" 
                 ItemsSource="{Binding Lines}" 
                 SelectedItem="{Binding SelectedLine}"
                 AutoGenerateColumns="False" 
                 IsReadOnly="True"
                 SelectionMode="Single"
                 CanUserAddRows="False"
                 GridLinesVisibility="Horizontal"
                 HeadersVisibility="Column"
                 AlternatingRowBackground="#F9F9F9"
                 RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الهاتف" Binding="{Binding PhoneNumber}" Width="150"/>
                <DataGridTextColumn Header="الرقم التسلسلي" Binding="{Binding SerialNumber}" Width="150"/>
                <DataGridTextColumn Header="المشغل" Binding="{Binding Category.Operator.Name}" Width="100"/>
                <DataGridTextColumn Header="الفئة" Binding="{Binding Category.Name}" Width="150"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="المحفظة" Binding="{Binding WalletId}" Width="120"/>
                <DataGridTextColumn Header="المخصص له" Binding="{Binding AssignedTo.FullName}" Width="150"/>
                <DataGridTextColumn Header="تاريخ الإضافة" Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Add/Edit Form -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="15" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" 
                          Text="{Binding IsEditing, Converter={StaticResource BoolToTextConverter}, ConverterParameter='تعديل خط|إضافة خط جديد'}" 
                          FontSize="18" 
                          FontWeight="SemiBold" 
                          Margin="0,0,0,15"/>
                
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Column="0" Margin="0,0,10,10">
                        <TextBlock Text="المشغل" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding Operators}" 
                                 SelectedValue="{Binding SelectedOperatorId}" 
                                 SelectedValuePath="Id" 
                                 DisplayMemberPath="Name" 
                                 Padding="8"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="الفئة" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding Categories}" 
                                 SelectedValue="{Binding SelectedCategoryId}" 
                                 SelectedValuePath="Id" 
                                 DisplayMemberPath="Name" 
                                 Padding="8"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Margin="0,0,0,10">
                        <TextBlock Text="رقم الهاتف" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" 
                                Padding="8"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
                        <TextBlock Text="الرقم التسلسلي" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SerialNumber, UpdateSourceTrigger=PropertyChanged}" 
                                Padding="8"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="رقم المحفظة" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding WalletId, UpdateSourceTrigger=PropertyChanged}" 
                                Padding="8"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,10">
                        <TextBlock Text="ملاحظات" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" 
                                Padding="8"/>
                    </StackPanel>
                </Grid>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="إضافة" 
                           Command="{Binding AddLineCommand}" 
                           Style="{StaticResource PrimaryButton}" 
                           Margin="0,0,10,0" 
                           Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>
                    <Button Content="حفظ التعديلات" 
                           Command="{Binding SaveLineCommand}" 
                           Style="{StaticResource PrimaryButton}" 
                           Margin="0,0,10,0" 
                           Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <Button Content="تعديل" 
                           Command="{Binding EditLineCommand}" 
                           Style="{StaticResource SecondaryButton}" 
                           Margin="0,0,10,0"/>
                    <Button Content="حذف" 
                           Command="{Binding DeleteLineCommand}" 
                           Style="{StaticResource SecondaryButton}" 
                           Margin="0,0,10,0"/>
                    <Button Content="إلغاء" 
                           Command="{Binding CancelEditCommand}" 
                           Style="{StaticResource SecondaryButton}" 
                           Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
