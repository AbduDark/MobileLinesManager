
<Window x:Class="MobileLinesManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MobileLinesManager"
        xmlns:vm="clr-namespace:MobileLinesManager.ViewModels"
        mc:Ignorable="d"
        Title="إدارة خطوط الموبايل" 
        Height="768" 
        Width="1280"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Language="ar-SA">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#0078D4">
            <Grid>
                <TextBlock Text="إدارة خطوط الموبايل" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          VerticalAlignment="Center" 
                          Margin="20,0"/>
                
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Left" 
                           VerticalAlignment="Center" 
                           Margin="20,0">
                    <TextBlock Text="التنبيهات:" 
                              Foreground="White" 
                              FontSize="14" 
                              VerticalAlignment="Center" 
                              Margin="0,0,10,0"/>
                    <Border Background="#E74856" 
                           CornerRadius="12" 
                           Padding="8,4" 
                           MinWidth="30">
                        <TextBlock Text="{Binding AlertCount}" 
                                  Foreground="White" 
                                  FontWeight="Bold" 
                                  HorizontalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Navigation Menu -->
        <Border Grid.Row="1" Grid.Column="0" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <Button Content="لوحة التحكم" 
                       Command="{Binding NavigateCommand}" 
                       CommandParameter="Dashboard"
                       Style="{StaticResource SecondaryButton}" 
                       Margin="0,5" 
                       HorizontalAlignment="Stretch"/>
                
                <Button Content="الخطوط" 
                       Command="{Binding NavigateCommand}" 
                       CommandParameter="Lines"
                       Style="{StaticResource SecondaryButton}" 
                       Margin="0,5" 
                       HorizontalAlignment="Stretch"/>
                
                <Button Content="التسليمات" 
                       Command="{Binding NavigateCommand}" 
                       CommandParameter="Assignments"
                       Style="{StaticResource SecondaryButton}" 
                       Margin="0,5" 
                       HorizontalAlignment="Stretch"/>
                
                <Button Content="الفئات" 
                       Command="{Binding NavigateCommand}" 
                       CommandParameter="Categories"
                       Style="{StaticResource SecondaryButton}" 
                       Margin="0,5" 
                       HorizontalAlignment="Stretch"/>
                
                <Button Content="التقارير" 
                       Command="{Binding NavigateCommand}" 
                       CommandParameter="Reports"
                       Style="{StaticResource SecondaryButton}" 
                       Margin="0,5" 
                       HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ContentControl Grid.Row="1" Grid.Column="1" Margin="10">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CurrentView}" Value="Dashboard">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <local:DashboardView/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentView}" Value="Lines">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <local:LinesView/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentView}" Value="Assignments">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <local:AssignmentsView/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentView}" Value="Categories">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <local:CategoriesView/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentView}" Value="Reports">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <local:ReportsView/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</Window>
